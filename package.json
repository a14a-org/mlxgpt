{
	"name": "mlxgpt-site",
	"version": "0.1.0",
	"private": true,
	"description": "Tooling and end-to-end tests for the mlxgpt.com static site.",
	"scripts": {
		"serve": "bunx serve site --listen 4173 --no-clipboard --single",
		"lint": "biome check playwright.config.ts tsconfig.json package.json tests/e2e",
		"typecheck": "tsc --noEmit",
		"test:e2e": "playwright test",
		"test:smoke": "playwright test"
	},
	"devDependencies": {
		"@biomejs/biome": "2.5.2",
		"@playwright/test": "1.61.1",
		"@types/node": "26.1.1",
		"serve": "14.2.6",
		"typescript": "7.0.2"
	}
}
